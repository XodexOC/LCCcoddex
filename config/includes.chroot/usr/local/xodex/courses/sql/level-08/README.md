# Lab 08: Views & subqueries / Представления и подзапросы

**Track:** SQL & data / SQL и данные  
**Slug:** `08-views-subqueries`

## Task (EN)

View of active users; query it.

## Задание (RU)

VIEW активных пользователей; запрос к нему.

## Workflow

1. Read `docs/sql/en/08-*.md` and `docs/sql/ru/08-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
