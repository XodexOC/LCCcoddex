# Lab 04: JOINs / JOIN

**Track:** SQL & data / SQL и данные  
**Slug:** `04-joins`

## Task (EN)

Join orders and customers; list orders without customers (LEFT).

## Задание (RU)

JOIN orders/customers; заказы без клиента (LEFT).

## Workflow

1. Read `docs/sql/en/04-*.md` and `docs/sql/ru/04-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
