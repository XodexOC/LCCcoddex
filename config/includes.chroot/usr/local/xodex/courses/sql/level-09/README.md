# Lab 09: SQLite from apps / SQLite из приложений

**Track:** SQL & data / SQL и данные  
**Slug:** `09-app-sqlite`

## Task (EN)

Python script inserting users safely with placeholders.

## Задание (RU)

Python-скрипт с безопасными placeholders.

## Workflow

1. Read `docs/sql/en/09-*.md` and `docs/sql/ru/09-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
