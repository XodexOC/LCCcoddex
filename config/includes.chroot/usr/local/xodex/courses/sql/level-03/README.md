# Lab 03: Types & constraints / Типы и ограничения

**Track:** SQL & data / SQL и данные  
**Slug:** `03-types-constraints`

## Task (EN)

Create table with PK and UNIQUE email; try violating it.

## Задание (RU)

Таблица с PK и UNIQUE email; попробуй нарушить.

## Workflow

1. Read `docs/sql/en/03-*.md` and `docs/sql/ru/03-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
