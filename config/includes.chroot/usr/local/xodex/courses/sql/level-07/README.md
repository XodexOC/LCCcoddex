# Lab 07: Normalization / Нормализация

**Track:** SQL & data / SQL и данные  
**Slug:** `07-normalization`

## Task (EN)

Refactor a wide flat table into two related tables.

## Задание (RU)

Разбей «широкую» таблицу на две связанные.

## Workflow

1. Read `docs/sql/en/07-*.md` and `docs/sql/ru/07-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
