# Lab 03: Functions & stack / Функции и стек

**Track:** C Programming / Программирование на C  
**Slug:** `03-functions`

## Task (EN)

Implement factorial both iterative and recursive; compare for n=10.

## Задание (RU)

Факториал итеративно и рекурсивно; сравни для n=10.

## Workflow

1. Read `docs/c/en/03-*.md` and `docs/c/ru/03-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
