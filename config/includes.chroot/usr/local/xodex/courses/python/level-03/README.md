# Lab 03: Functions / Функции

**Track:** Python / Python  
**Slug:** `03-functions`

## Task (EN)

Implement map/filter equivalents with list comprehensions.

## Задание (RU)

Аналоги map/filter через list comprehension.

## Workflow

1. Read `docs/python/en/03-*.md` and `docs/python/ru/03-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
