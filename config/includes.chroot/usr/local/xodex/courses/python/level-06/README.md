# Lab 06: Errors & files / Ошибки и файлы

**Track:** Python / Python  
**Slug:** `06-errors-files`

## Task (EN)

Read JSON config with clear errors if missing keys.

## Задание (RU)

Чтение JSON-конфига с понятными ошибками.

## Workflow

1. Read `docs/python/en/06-*.md` and `docs/python/ru/06-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
