# Lab 07: Iterators & generators / Итераторы и генераторы

**Track:** Python / Python  
**Slug:** `07-iterators`

## Task (EN)

Generator that yields Fibonacci until limit.

## Задание (RU)

Генератор Фибоначчи до предела.

## Workflow

1. Read `docs/python/en/07-*.md` and `docs/python/ru/07-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
