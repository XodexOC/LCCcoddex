# Lab 00: Hello & REPL / Hello и REPL

**Track:** Python / Python  
**Slug:** `00-hello-repl`

## Task (EN)

Run hello.py and explore help() and dir() in the REPL.

## Задание (RU)

Запусти hello.py; в REPL изучи help() и dir().

## Workflow

1. Read `docs/python/en/00-*.md` and `docs/python/ru/00-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
