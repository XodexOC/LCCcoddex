# Lab 01: Types & operators / Типы и операторы

**Track:** Python / Python  
**Slug:** `01-types-ops`

## Task (EN)

Write expressions that show float imprecision and integer unlimited size.

## Задание (RU)

Покажи неточность float и «бесконечный» int.

## Workflow

1. Read `docs/python/en/01-*.md` and `docs/python/ru/01-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
