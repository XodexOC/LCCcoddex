# Lab 05: Collections / Коллекции

**Track:** Rust / Rust  
**Slug:** `05-collections`

## Task (EN)

Word count with HashMap<String, u32>.

## Задание (RU)

Подсчёт слов через HashMap<String, u32>.

## Workflow

1. Read `docs/rust/en/05-*.md` and `docs/rust/ru/05-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
