# Lab 02: References & borrowing / Ссылки и borrowing

**Track:** Rust / Rust  
**Slug:** `02-borrowing`

## Task (EN)

Function that takes &str and returns length; one that mutates via &mut.

## Задание (RU)

Функция &str→len; функция с &mut.

## Workflow

1. Read `docs/rust/en/02-*.md` and `docs/rust/ru/02-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
