# Lab 04: Error handling / Ошибки

**Track:** Rust / Rust  
**Slug:** `04-error-handling`

## Task (EN)

Read a file to String; propagate errors with ? from main via Result.

## Задание (RU)

Чтение файла; ? из main через Result.

## Workflow

1. Read `docs/rust/en/04-*.md` and `docs/rust/ru/04-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
