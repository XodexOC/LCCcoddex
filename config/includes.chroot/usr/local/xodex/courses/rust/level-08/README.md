# Lab 08: Tests / Тесты

**Track:** Rust / Rust  
**Slug:** `08-testing`

## Task (EN)

Unit tests for a pure function; one edge case.

## Задание (RU)

Unit-тесты чистой функции; граничный случай.

## Workflow

1. Read `docs/rust/en/08-*.md` and `docs/rust/ru/08-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
