# Урок 00: Введение в SQL

SQL (Structured Query Language) — стандартный язык для работы
с реляционными базами данных.

## Основные операции

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT
);

INSERT INTO users VALUES (1, 'Xodex');
SELECT * FROM users;
```
