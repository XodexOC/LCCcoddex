# Lesson 00: Introduction to SQL

SQL (Structured Query Language) is the standard language
for working with relational databases.

## Basic operations

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT
);

INSERT INTO users VALUES (1, 'Xodex');
SELECT * FROM users;
```
