# Lab 07: Structs & data layout / Структуры и layout

**Track:** C Programming / Программирование на C  
**Slug:** `07-structs`

## Task (EN)

Define Point and Rectangle; compute area; print sizeof and offsets with offsetof.

## Задание (RU)

Point и Rectangle; площадь; sizeof и offsetof.

## Workflow

1. Read `docs/c/en/07-*.md` and `docs/c/ru/07-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
