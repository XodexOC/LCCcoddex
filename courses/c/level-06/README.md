# Lab 06: Dynamic memory / Динамическая память

**Track:** C Programming / Программирование на C  
**Slug:** `06-memory`

## Task (EN)

Build a dynamic int array (push). Run under valgrind --leak-check=full.

## Задание (RU)

Динамический массив int (push). Проверь valgrind --leak-check=full.

## Workflow

1. Read `docs/c/en/06-*.md` and `docs/c/ru/06-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
