# Lab 00: Hello World & toolchain / Hello World и toolchain

**Track:** C Programming / Программирование на C  
**Slug:** `00-hello-world`

## Task (EN)

Compile and run hello.c. Explain what gcc, as, and ld do at a high level.

## Задание (RU)

Скомпилируй и запусти hello.c. Кратко объясни роль gcc, as и ld.

## Workflow

1. Read `docs/c/en/00-*.md` and `docs/c/ru/00-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
