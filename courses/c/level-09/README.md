# Lab 09: Preprocessor & Make / Препроцессор и Make

**Track:** C Programming / Программирование на C  
**Slug:** `09-preprocessor-make`

## Task (EN)

Split a mini project into .h/.c with Makefile and -Wall -Wextra -Werror.

## Задание (RU)

Разбей мини-проект на .h/.c, Makefile с -Wall -Wextra -Werror.

## Workflow

1. Read `docs/c/en/09-*.md` and `docs/c/ru/09-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
