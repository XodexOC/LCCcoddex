# Lab 05: Arrays & strings / Массивы и строки

**Track:** C Programming / Программирование на C  
**Slug:** `05-arrays-strings`

## Task (EN)

Implement safe_strlen and reverse a string in place.

## Задание (RU)

Реализуй safe_strlen и разворот строки in-place.

## Workflow

1. Read `docs/c/en/05-*.md` and `docs/c/ru/05-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
