# Lab 07: Modules & crates / Модули и crates

**Track:** Rust / Rust  
**Slug:** `07-modules`

## Task (EN)

Split code into lib + bin calling a public function.

## Задание (RU)

lib + bin с публичной функцией.

## Workflow

1. Read `docs/rust/en/07-*.md` and `docs/rust/ru/07-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
