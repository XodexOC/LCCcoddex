# Lab 03: Types & match / Типы и match

**Track:** Rust / Rust  
**Slug:** `03-types-match`

## Task (EN)

Enum Shape { Circle, Rect } with area method via match.

## Задание (RU)

Enum Shape с area через match.

## Workflow

1. Read `docs/rust/en/03-*.md` and `docs/rust/ru/03-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
