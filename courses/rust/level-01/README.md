# Lab 01: Ownership / Ownership

**Track:** Rust / Rust  
**Slug:** `01-ownership`

## Task (EN)

Show a compile error from use-after-move; fix with clone or borrow.

## Задание (RU)

Ошибка use-after-move; исправь clone или borrow.

## Workflow

1. Read `docs/rust/en/01-*.md` and `docs/rust/ru/01-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
