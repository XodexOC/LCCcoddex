# Lab 02: Users & sudo / Пользователи и sudo

**Track:** Linux Systems / Системы Linux  
**Slug:** `02-users-sudo`

## Task (EN)

As xodex, run a command with sudo and inspect /etc/sudoers.d/xodex.

## Задание (RU)

Под xodex выполни sudo и посмотри /etc/sudoers.d/xodex.

## Workflow

1. Read `docs/linux/en/02-*.md` and `docs/linux/ru/02-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
