# Lab 08: systemd & services / systemd и сервисы

**Track:** Linux Systems / Системы Linux  
**Slug:** `08-systemd`

## Task (EN)

Inspect getty@tty1 and explain Xodex autologin drop-in.

## Задание (RU)

Изучи getty@tty1 и drop-in автологина Xodex.

## Workflow

1. Read `docs/linux/en/08-*.md` and `docs/linux/ru/08-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
