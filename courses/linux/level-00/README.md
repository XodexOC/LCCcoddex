# Lab 00: Shell survival / Выживание в shell

**Track:** Linux Systems / Системы Linux  
**Slug:** `00-shell-survival`

## Task (EN)

Navigate /usr/local/xodex and document five useful paths.

## Задание (RU)

Пройди /usr/local/xodex и опиши пять полезных путей.

## Workflow

1. Read `docs/linux/en/00-*.md` and `docs/linux/ru/00-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
