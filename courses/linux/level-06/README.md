# Lab 06: Packages (apt) / Пакеты (apt)

**Track:** Linux Systems / Системы Linux  
**Slug:** `06-packages`

## Task (EN)

Search a package with apt-cache and explain dependencies of git.

## Задание (RU)

Найди пакет apt-cache; разбери зависимости git.

## Workflow

1. Read `docs/linux/en/06-*.md` and `docs/linux/ru/06-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
