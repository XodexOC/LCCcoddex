# Lab 05: Permissions & links / Права и ссылки

**Track:** Linux Systems / Системы Linux  
**Slug:** `05-permissions-links`

## Task (EN)

Create a private directory mode 700 and a symlink to a lesson file.

## Задание (RU)

Каталог 700 и symlink на урок.

## Workflow

1. Read `docs/linux/en/05-*.md` and `docs/linux/ru/05-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
