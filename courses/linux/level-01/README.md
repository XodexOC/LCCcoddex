# Lab 01: Filesystem hierarchy / Иерархия ФС

**Track:** Linux Systems / Системы Linux  
**Slug:** `01-filesystem`

## Task (EN)

Explain permissions of /etc/passwd and your home directory.

## Задание (RU)

Объясни права /etc/passwd и своего home.

## Workflow

1. Read `docs/linux/en/01-*.md` and `docs/linux/ru/01-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
