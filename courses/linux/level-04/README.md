# Lab 04: Pipes & text tools / Пайпы и текст

**Track:** Linux Systems / Системы Linux  
**Slug:** `04-pipes-text`

## Task (EN)

Count unique words in a file with a pipeline.

## Задание (RU)

Уникальные слова в файле одним пайплайном.

## Workflow

1. Read `docs/linux/en/04-*.md` and `docs/linux/ru/04-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
