# Lab 02: INSERT/UPDATE/DELETE / INSERT/UPDATE/DELETE

**Track:** SQL & data / SQL и данные  
**Slug:** `02-insert-update`

## Task (EN)

Insert three rows; update one; delete one inside a transaction.

## Задание (RU)

Три INSERT; UPDATE; DELETE в транзакции.

## Workflow

1. Read `docs/sql/en/02-*.md` and `docs/sql/ru/02-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
