# Lab 10: Data literacy (capstone) / Грамотность данных (итог)

**Track:** SQL & data / SQL и данные  
**Slug:** `10-data-literacy`

## Task (EN)

Design a mini schema for courses/lessons/progress; implement in SQLite.

## Задание (RU)

Схема courses/lessons/progress; реализация в SQLite.

## Workflow

1. Read `docs/sql/en/10-*.md` and `docs/sql/ru/10-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
