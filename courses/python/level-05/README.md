# Lab 05: OOP / ООП

**Track:** Python / Python  
**Slug:** `05-oop`

## Task (EN)

Model BankAccount with deposit/withdraw; raise on insufficient funds.

## Задание (RU)

BankAccount: deposit/withdraw; ошибка при нехватке средств.

## Workflow

1. Read `docs/python/en/05-*.md` and `docs/python/ru/05-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
