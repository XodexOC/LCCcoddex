# Lab 02: Control & collections / Управление и коллекции

**Track:** Python / Python  
**Slug:** `02-control-collections`

## Task (EN)

Word frequency counter over a text file using dict.

## Задание (RU)

Частоты слов в файле через dict.

## Workflow

1. Read `docs/python/en/02-*.md` and `docs/python/ru/02-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
