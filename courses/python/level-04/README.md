# Lab 04: Modules & venv / Модули и venv

**Track:** Python / Python  
**Slug:** `04-modules-venv`

## Task (EN)

Create a venv, install pytest, write a tiny package with two modules.

## Задание (RU)

venv, pytest, мини-пакет из двух модулей.

## Workflow

1. Read `docs/python/en/04-*.md` and `docs/python/ru/04-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
