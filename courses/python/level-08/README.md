# Lab 08: Testing with pytest / Тесты pytest

**Track:** Python / Python  
**Slug:** `08-testing`

## Task (EN)

Write tests for a pure function; make one fail then fix.

## Задание (RU)

Тесты чистой функции; один fail, затем fix.

## Workflow

1. Read `docs/python/en/08-*.md` and `docs/python/ru/08-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
