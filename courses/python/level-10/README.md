# Lab 10: Projects & style (capstone) / Проекты и стиль (итог)

**Track:** Python / Python  
**Slug:** `10-projects`

## Task (EN)

Build a mini todo CLI with JSON storage and tests.

## Задание (RU)

Мини todo CLI с JSON и тестами.

## Workflow

1. Read `docs/python/en/10-*.md` and `docs/python/ru/10-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
