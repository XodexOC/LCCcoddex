# Lab 09: stdlib & CLI / stdlib и CLI

**Track:** Python / Python  
**Slug:** `09-stdlib-cli`

## Task (EN)

CLI that greps a pattern in files using only stdlib.

## Задание (RU)

CLI-grep по файлам только на stdlib.

## Workflow

1. Read `docs/python/en/09-*.md` and `docs/python/ru/09-*.md`.
2. Implement your solution in this directory (`solution.*`).
3. Run `./test.sh`.
4. Mark the lesson complete in `~/.xodex/progress.json` when ready.

## Tools

Use what the Live image provides: `gcc`, `python3`, `rustc`/`cargo`, `sqlite3`, `man`, `valgrind`, etc.
